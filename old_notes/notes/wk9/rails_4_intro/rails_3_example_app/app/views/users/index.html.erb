<ul class="users">
	<% @users.each do | user | %>
	<div class="user">
		<h2><%= user.name %></h2>
		<p><%= user.bio %></p>
		<p><%= link_to "edit", edit_user_path(user) %></p>
	</div>
	<% end %>
</ul>